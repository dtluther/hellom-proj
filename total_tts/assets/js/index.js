import React, { Component } from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return  (
        <div>React is working in Django!</div>
    );
}

ReactDOM.render(<App />, document.getElementById('root'));